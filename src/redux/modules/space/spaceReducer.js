const spaceReducer = (state = {}, action) => {
  switch (action.type) {
    case 'CREATE_SPACE':
      break;
    case 'DELETE_SPACE':
      break;
    default:
      break;
  }
  return state;
};

export default spaceReducer;